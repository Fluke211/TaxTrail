export function prorateCents(totalCents: number, weights: number[]): number[];
export function splitSalesTax(
  salesTax: number | null,
  weightsCents: number[],
  totalCents: number
): (number | null)[];
